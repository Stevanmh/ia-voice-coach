services:
  voice-coach-bot:
    build: .
    container_name: voice-coach-bot
    restart: always
    ports:
      - "3005:3000"
    env_file: .env
    volumes:
      - ./temp:/app/temp
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
